# Leave empty to call /api via Vite dev proxy (see vite.config.ts)
VITE_API_URL=
# Backend URL used by Vite proxy in development.
# Default: local backend. Switch to EC2 to develop against production:
#   VITE_API_PROXY_TARGET=http://ec2-52-62-106-186.ap-southeast-2.compute.amazonaws.com
VITE_API_PROXY_TARGET=http://localhost:5076
