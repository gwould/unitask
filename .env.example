# Production (Vercel): set full API URL, e.g. https://api.example.com or http://52.62.106.186
# Development: leave empty and use Vite proxy (npm run dev)
VITE_API_URL=

# Proxy target when VITE_API_URL is empty (default launch profile port)
VITE_API_PROXY_TARGET=http://localhost:5076
